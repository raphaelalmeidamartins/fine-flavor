const INITIAL_STATE = {
  results: [],
  searchBar: {
    display: false,
  },
  loading: true,
};

const search = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default search;
